{
  "endpoint": "mu.getById",
  "namespace": "mu",
  "method": "getById",
  "auth": "optional",
  "description": "Returns a military unit object by its ID.",
  "input": {
    "muId": {
      "type": "string",
      "required": true,
      "description": "Unique identifier of the military unit."
    }
  },
  "output": {
    "description": "Military unit object with identity and membership data.",
    "fields": {
      "_id": "string — military unit identifier",
      "name": "string — military unit name",
      "country": "string — countryId the MU belongs to",
      "memberCount": "number — number of members",
      "meta": "object — additional MU metadata"
    }
  },
  "notes": "MU is short for military unit. Authentication may expose additional internal fields."
}
