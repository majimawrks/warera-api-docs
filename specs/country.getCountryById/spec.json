{
  "endpoint": "country.getCountryById",
  "namespace": "country",
  "method": "getCountryById",
  "auth": "none",
  "description": "Returns a full country object including production bonuses, government info, and economy data.",
  "input": {
    "countryId": {
      "type": "string",
      "required": true,
      "description": "Unique identifier of the country."
    }
  },
  "output": {
    "description": "Full country object with economy, government, and territorial data.",
    "fields": {
      "_id": "string — country identifier",
      "name": "string — country name",
      "productionBonuses": "object — production bonus values by resource type",
      "government": "object — current government and president info",
      "economy": "object — economic statistics",
      "regions": "array — list of region IDs belonging to this country"
    }
  },
  "notes": "No authentication required."
}
